{
  "name": "nabung-app",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "dev:migrate": "bunx prisma migrate dev --name update && bun run generate",
    "generate": "bunx prisma generate",
    "db:push": "bunx prisma db push && bun run generate",
    "start": "bun run src/server.ts"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.5.0",
    "@prisma/client": "^6.6.1",
    "bcryptjs": "^3.0.2",
    "hono": "^4.7.8",
    "ioredis": "^5.6.1",
    "nanoid": "^5.1.5",
    "nodemailer": "^7.0.3",
    "prisma": "^6.6.1",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/nodemailer": "^6.4.17"
  }
}
